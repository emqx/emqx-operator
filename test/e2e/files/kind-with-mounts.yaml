apiVersion: kind.x-k8s.io/v1alpha4
kind: Cluster
name: test-e2e
nodes:
- role: control-plane
  # add a mount from /path/to/my/files on the host to /files on the node
  extraMounts:
  - hostPath: /tmp/kind/data
    containerPath: /tmp/kind/data
